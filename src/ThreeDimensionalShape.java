
public interface ThreeDimensionalShape extends Shape{

    /**
     * every three dimensional shape as a volume they must define
     * @return the volume of the three dimensional shape
     */
    public abstract double getVolume();
}
